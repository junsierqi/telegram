[project]
name = "telegram-like-server"
version = "0.1.0"
description = "Python control-plane server skeleton for a chat-first product with future remote control"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
