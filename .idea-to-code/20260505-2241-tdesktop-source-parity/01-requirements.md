# Requirements

REQ-1 functional: Use the local Telegram Desktop source tree at `D:\code\tdesktop` as the primary implementation reference for desktop UI parity.
REQ-2 functional: Map tdesktop style/layout constants into this project's Qt desktop UI instead of relying only on screenshot guesses.
REQ-3 functional: Preserve existing reference screenshot validators and desktop smoke behavior while tightening UI parity.
REQ-4 constraint: Do not push commits; commit only when explicitly requested.
